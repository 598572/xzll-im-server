<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzll.business.mapper.ImGroupMemberMapper">

    <!-- 批量插入群成员（性能优化） -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO im_group_member (
            group_id, user_id, member_role, join_time, join_type, status, create_time, update_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.groupId},
                #{item.userId},
                #{item.memberRole},
                #{item.joinTime},
                #{item.joinType},
                #{item.status},
                #{item.createTime},
                #{item.updateTime}
            )
        </foreach>
    </insert>

</mapper>
