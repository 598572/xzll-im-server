# IM Gatling 压测模块 - 完整使用说明

## 📦 模块结构

```
im-stress-gatling/
├── pom.xml                                    # Maven 配置
├── src/
│   └── test/
│       └── scala/
│           └── com/xzll/stress/
│               └── ImWebSocketSimulation.scala  # 压测脚本（Scala）
├── run-stress-test.sh                         # 快速启动脚本
├── README.md                                  # 详细文档
├── QUICK_START.md                             # 快速开始指南
└── 压测模块使用说明.md                        # 本文件
```

## 🎯 核心功能

### 1. 压测脚本（ImWebSocketSimulation.scala）

**这是唯一需要关注的 Scala 文件**，主要功能：

```scala
// 1. 建立 WebSocket 连接
ws("Connect WebSocket")
  .connect(s"$wsUrl?userId=$${userId}")
  .header("token", testToken)
  .header("uid", "${userId}")

// 2. 发送心跳（每 30 秒）
ws("Send Ping Heartbeat").sendBytes(...)

// 3. 发送单聊消息（每 5 秒）
ws("Send C2C Message").sendBytes(buildC2CMessage(...))

// 4. 关闭连接
ws("Close WebSocket").close
```

**Protobuf 消息构建**（复用你的 im-common）：

```scala
def buildC2CMessage(from: String, to: String, content: String): Array[Byte] = {
  // 1. 构建 C2CSendReq
  val sendReq = C2CSendReq.newBuilder()
    .setClientMsgId(ProtoConverterUtil.uuidStringToBytes(UUID.randomUUID().toString))
    .setFrom(ProtoConverterUtil.snowflakeStringToLong(from))
    .setTo(ProtoConverterUtil.snowflakeStringToLong(to))
    .setFormat(MsgFormatEnum.TEXT_MSG.getCode)
    .setContent(content)
    .setTime(System.currentTimeMillis())
    .build()
  
  // 2. 包装为 ImProtoRequest
  val protoRequest = ImProtoRequest.newBuilder()
    .setType(MsgType.C2C_SEND)
    .setPayload(ByteString.copyFrom(sendReq.toByteArray))
    .build()
  
  protoRequest.toByteArray
}
```

### 2. 快速启动脚本（run-stress-test.sh）

**一键启动压测**，自动处理：
- ✅ 环境检查（Java、Maven）
- ✅ 参数配置
- ✅ 编译项目
- ✅ 启动压测
- ✅ 自动打开报告

## 🚀 三种使用方式

### 方式一：脚本启动（推荐）

```bash
# 1. 赋予执行权限（首次）
chmod +x run-stress-test.sh

# 2. 默认配置（1 万用户，2 分钟启动，测试 10 分钟）
./run-stress-test.sh

# 3. 自定义配置
./run-stress-test.sh 50000 300 1800
#                    ↑      ↑   ↑
#                    用户数  启动时间(秒)  测试时长(秒)

# 4. 指定服务器
TARGET_HOST=okim.site TARGET_PORT=10001 ./run-stress-test.sh
```

### 方式二：Maven 命令

```bash
# 默认配置
mvn gatling:test

# 完整自定义
mvn gatling:test \
  -DTARGET_HOST=120.46.85.43 \
  -DTARGET_PORT=80 \
  -DMACHINE_ID=0 \
  -DUSERS_PER_MACHINE=10000 \
  -DRAMP_UP_TIME=120 \
  -DTEST_DURATION=600 \
  -DTEST_TOKEN="你的Token"
```

### 方式三：IDEA 中运行

1. 打开 `ImWebSocketSimulation.scala`
2. 右键 → Run 'ImWebSocketSimulation'
3. 或者在 Maven 面板中找到 `gatling:test` 双击运行

## 📊 压测流程

```
┌─────────────────────────────────────────────────────────────┐
│                     压测流程时间线                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  0s ────────────────────────────────────────────────────────│
│   │                                                          │
│   │  阶段 1: 连接建立（Ramp Up）                            │
│   │  ┌───────────────────────────────────────┐             │
│   │  │ 逐步启动 10000 个虚拟用户              │             │
│   │  │ 每个用户建立 WebSocket 连接            │             │
│   │  └───────────────────────────────────────┘             │
│   │                                                          │
│ 120s ───────────────────────────────────────────────────────│
│   │                                                          │
│   │  阶段 2: 压测运行（Test Duration）                      │
│   │  ┌───────────────────────────────────────┐             │
│   │  │ 每个用户每 30 秒发送心跳               │             │
│   │  │ 每个用户每 5 秒发送单聊消息            │             │
│   │  │ 接收服务端推送的消息                   │             │
│   │  └───────────────────────────────────────┘             │
│   │                                                          │
│ 720s ───────────────────────────────────────────────────────│
│   │                                                          │
│   │  阶段 3: 连接关闭                                        │
│   │  ┌───────────────────────────────────────┐             │
│   │  │ 所有用户优雅关闭连接                   │             │
│   │  └───────────────────────────────────────┘             │
│   │                                                          │
│ 750s ───────────────────────────────────────────────────────│
│                                                              │
│  压测结束，生成报告                                          │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 关键配置说明

### 服务端认证配置（重要！）

压测时需要绕过认证，**推荐在服务端添加后门**：

编辑 `im-connect/im-connect-service/src/main/java/com/xzll/connect/netty/handler/AuthHandler.java`：

```java
private boolean performAuthentication(ChannelHandlerContext ctx, FullHttpRequest request, String clientIp) {
    HttpHeaders headers = request.headers();
    String token = headers.get(ImConstant.TOKEN);
    
    // ========== 【新增】压测后门 ==========
    if ("stress_test_token_bypass".equals(token)) {
        String uid = headers.get("uid");
        if (StringUtils.isNotBlank(uid)) {
            ctx.channel().attr(ImConstant.USER_ID_KEY).setIfAbsent(uid);
            log.info("✅ 压测用户认证通过：uid={}", uid);
            return true;
        }
    }
    // ====================================
    
    // 正常认证流程...
    if (StringUtils.isEmpty(token)) {
        log.warn("Token为空，认证失败：{}", clientIp);
        handleAuthFailure(ctx, clientIp, "Token为空");
        return false;
    }
    
    String uid = validateToken(token);
    // ...
}
```

然后重启 im-connect 服务，压测时会自动使用后门 Token。

### 用户 ID 生成规则

```scala
// 用户 ID = 基础 ID + 机器偏移 + 虚拟用户编号
val userId = s"${1249485670040L + userIdOffset + session.userId}"

// 示例：
// 机器 0，用户 0：1249485670040
// 机器 0，用户 1：1249485670041
// 机器 1，用户 0：1249485680040（假设 usersPerMachine=10000）
```

这样确保**每个虚拟用户的 ID 全局唯一**。

### 消息发送逻辑

```scala
// 随机选择接收方（从所有压测用户中随机）
val toUserIndex = scala.util.Random.nextInt(usersPerMachine * 10)
val toUser = s"${1249485670040L + toUserIndex}"

// 构建消息
val message = buildC2CMessage(fromUser, toUser, "Stress test message")

// 发送
ws("Send C2C Message").sendBytes(message)
```

## 📈 报告解读

### 关键指标

| 指标 | 位置 | 说明 | 健康值 |
|------|------|------|--------|
| **Requests** | Global Information | 总请求数 | - |
| **OK** | Global Information | 成功请求数 | > 95% |
| **KO** | Global Information | 失败请求数 | < 5% |
| **Mean** | Response Time | 平均响应时间 | < 200ms |
| **P95** | Response Time | 95% 响应时间 | < 500ms |
| **P99** | Response Time | 99% 响应时间 | < 1000ms |
| **Max** | Response Time | 最大响应时间 | < 5000ms |
| **Requests/s** | Throughput | 每秒请求数 | 越高越好 |

### 报告示例解读

```
Global Information
├── Total requests: 120000
├── OK: 118500 (98.75%)
├── KO: 1500 (1.25%)
└── Mean response time: 156ms

Response Time Distribution
├── P50: 120ms
├── P75: 180ms
├── P95: 320ms
├── P99: 580ms
└── Max: 2100ms

Throughput
└── Requests/s: 2000
```

**解读**：
- ✅ 成功率 98.75%，健康
- ✅ P95 响应时间 320ms，优秀
- ✅ 吞吐量 2000 req/s，符合预期

## 🚨 常见问题排查

### 问题 1：连接成功率低（< 80%）

**症状**：报告中 KO 比例很高。

**可能原因**：
1. Token 认证失败
2. 服务器资源不足
3. 启动速度太快

**排查步骤**：
```bash
# 1. 检查服务端日志
tail -f /path/to/im-connect/logs/im-connect.log

# 2. 检查是否有 "Token无效" 或 "认证失败" 日志
grep "认证失败" /path/to/im-connect/logs/im-connect.log

# 3. 检查服务器资源
top  # 查看 CPU、内存
netstat -an | grep ESTABLISHED | wc -l  # 查看连接数
```

**解决方案**：
- 确认服务端已添加认证后门
- 增加 `RAMP_UP_TIME`（如 300 秒）
- 检查服务器 CPU、内存是否充足

### 问题 2：响应时间很长（P95 > 1s）

**症状**：报告中响应时间分布图右偏。

**可能原因**：
1. 服务器处理能力不足
2. Redis 性能瓶颈
3. 数据库查询慢

**排查步骤**：
```bash
# 1. 检查服务端 CPU
top -p $(pgrep -f im-connect)

# 2. 检查 Redis 性能
redis-cli --latency

# 3. 检查线程池队列
# 在服务端日志中搜索 "线程池队列过长"
```

**解决方案**：
- 优化服务端代码（减少 Redis 调用）
- 增加 Redis 内存
- 调整线程池大小

### 问题 3：压测机 CPU 100%

**症状**：压测机 CPU 占满，但用户数不到预期。

**可能原因**：压测机性能不足。

**解决方案**：
- 降低单机用户数
- 使用多台压测机（分布式）
- 增加压测机配置

## 🎓 进阶技巧

### 1. 修改心跳间隔

编辑 `ImWebSocketSimulation.scala`：

```scala
// 将 30 秒改为 60 秒
pace(60.seconds)
  .exec(ws("Send Ping Heartbeat").sendBytes(...))
```

### 2. 修改消息发送频率

```scala
// 将 5 秒改为 10 秒
.pause(10.seconds)
  .exec(ws("Send C2C Message").sendBytes(...))
```

### 3. 自定义负载模型

```scala
// 阶梯式负载
setUp(
  scn.inject(
    rampUsers(5000).during(60.seconds),   // 1 分钟到 5000
    constantUsersPerSec(100).during(300.seconds), // 保持 5 分钟
    rampUsers(10000).during(60.seconds)   // 再增加到 10000
  )
)

// 恒定负载
setUp(
  scn.inject(
    constantUsersPerSec(100).during(600.seconds) // 每秒 100 用户，持续 10 分钟
  )
)
```

### 4. 添加自定义指标

```scala
import io.gatling.core.Predef._

// 定义计数器
val msgSentCounter = new Counter("messages_sent")

// 在发送消息时增加计数
.exec { session =>
  msgSentCounter.increment()
  session
}
```

## 📚 参考资料

- [Gatling 官方文档](https://gatling.io/docs/gatling/)
- [WebSocket 压测指南](https://gatling.io/docs/gatling/reference/current/http/websocket/)
- [Scala 快速入门](https://docs.scala-lang.org/tour/tour-of-scala.html)

## 🆘 获取帮助

如果遇到问题：
1. 查看 [QUICK_START.md](QUICK_START.md) 快速开始指南
2. 查看 [README.md](README.md) 详细文档
3. 检查服务端日志
4. 提 Issue 或联系开发者

---

**祝压测顺利！** 🚀

